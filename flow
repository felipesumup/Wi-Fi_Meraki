<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Flow Executive Finders - Wi-Fi</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .container {
      background: #ffffff;
      border-radius: 16px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.4);
      max-width: 480px;
      width: 100%;
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, #FFD700, #FFC700);
      padding: 32px 24px;
      text-align: center;
      color: #1a1a1a;
    }

    .header .logo-icon {
      font-size: 48px;
      margin-bottom: 12px;
      display: block;
    }

    .header h1 {
      font-size: 24px;
      font-weight: 700;
      letter-spacing: 0.5px;
      color: #1a1a1a;
    }

    .header p {
      font-size: 14px;
      color: #333;
      margin-top: 4px;
    }

    .form-body {
      padding: 32px 28px;
    }

    .form-body p.intro {
      font-size: 14px;
      color: #555;
      margin-bottom: 24px;
      line-height: 1.6;
    }

    .form-group {
      margin-bottom: 18px;
    }

    label {
      display: block;
      font-size: 13px;
      font-weight: 600;
      color: #333;
      margin-bottom: 6px;
    }

    input[type="text"],
    input[type="email"],
    input[type="tel"] {
      width: 100%;
      padding: 11px 14px;
      border: 1.5px solid #d0d7de;
      border-radius: 8px;
      font-size: 14px;
      color: #222;
      transition: border-color 0.2s;
      outline: none;
      background: #fafafa;
    }

    input:focus {
      border-color: #FFD700;
      background: #fff;
      box-shadow: 0 0 0 3px rgba(255, 215, 0, 0.1);
    }

    input.error-field {
      border-color: #e53935;
    }

    .error-msg {
      color: #e53935;
      font-size: 12px;
      margin-top: 4px;
      display: none;
    }

    .mac-info {
      background: #fffacd;
      border: 1px solid #FFD700;
      border-radius: 8px;
      padding: 12px 14px;
      font-size: 13px;
      color: #333;
      margin-bottom: 20px;
    }

    .mac-info span {
      font-weight: 600;
      color: #1a1a1a;
    }

    button[type="submit"] {
      width: 100%;
      background: linear-gradient(135deg, #FFD700, #FFC700);
      color: #1a1a1a;
      border: none;
      border-radius: 8px;
      padding: 14px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: opacity 0.2s, transform 0.1s;
      margin-top: 6px;
    }

    button[type="submit"]:hover {
      opacity: 0.9;
      transform: translateY(-1px);
    }

    button[type="submit"]:active {
      transform: translateY(0);
    }

    .footer {
      text-align: center;
      padding: 14px 24px 20px;
      font-size: 11px;
      color: #aaa;
      border-top: 1px solid #f0f0f0;
    }

    .success-screen {
      display: none;
      padding: 48px 28px;
      text-align: center;
    }

    .success-screen .check {
      font-size: 64px;
      margin-bottom: 16px;
    }

    .success-screen h2 {
      font-size: 22px;
      font-weight: 700;
      color: #FFD700;
      margin-bottom: 10px;
    }

    .success-screen p {
      font-size: 14px;
      color: #555;
      line-height: 1.6;
    }

    button.loading {
      opacity: 0.7;
      cursor: not-allowed;
    }
  </style>
</head>
<body>

<div class="container">
  <div class="header">
    <span class="logo-icon">🏢</span>
    <h1>Flow Executive Finders</h1>
    <p>Rede de visitantes</p>
  </div>

  <div class="form-body" id="formSection">
    <p class="intro">
      Para acessar a internet, por favor preencha seus dados abaixo.
    </p>

    <div class="mac-info" id="macDisplay">
      🖥️ Dispositivo: <span id="macValue">detectando...</span>
    </div>

    <form id="wifiForm" novalidate>
      <div class="form-group">
        <label for="nome">Nome completo *</label>
        <input type="text" id="nome" placeholder="Seu nome completo" />
        <div class="error-msg" id="erroNome">Por favor, informe seu nome completo.</div>
      </div>

      <div class="form-group">
        <label for="email">E-mail *</label>
        <input type="email" id="email" placeholder="seu@email.com" />
        <div class="error-msg" id="erroEmail">Informe um e-mail válido.</div>
      </div>

      <div class="form-group">
        <label for="celular">Celular *</label>
        <input type="tel" id="celular" placeholder="(XX) 99999-9999" maxlength="15" />
        <div class="error-msg" id="erroCelular">Informe um celular válido (11 dígitos com DDD).</div>
      </div>

      <div class="form-group">
        <label for="cpf">CPF *</label>
        <input type="text" id="cpf" placeholder="000.000.000-00" maxlength="14" />
        <div class="error-msg" id="erroCpf">Informe um CPF válido.</div>
      </div>

      <button type="submit" id="btnConectar">🔓 Conectar ao Wi-Fi</button>
    </form>
  </div>

  <div class="success-screen" id="successScreen">
    <div class="check">✅</div>
    <h2>Conexão liberada!</h2>
    <p>Seus dados foram registrados com segurança.<br>Aproveite o acesso à internet.</p>
  </div>

  <div class="footer">
    Flow Executive Finders &copy; 2026 · Rede protegida
  </div>
</div>

<script>
  const APPS_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbyCPa_1SBUpwX6bzMlrwt2ptVJ9lEU3Cff8hGEHW0hsZ_-TqMYnlljjLEuby50hqgHT/exec";

  function getUrlParam(name) {
    const params = new URLSearchParams(window.location.search);
    return params.get(name) || "N/A";
  }

  const clientMac = getUrlParam("client_mac");
  document.getElementById("macValue").textContent = clientMac !== "N/A"
    ? clientMac
    : "não disponível (simulação)";

  document.getElementById("celular").addEventListener("input", function(e) {
    let value = e.target.value.replace(/\D/g, "");
    if (value.length > 11) value = value.slice(0, 11);
    
    if (value.length === 0) {
      e.target.value = "";
    } else if (value.length <= 2) {
      e.target.value = `(${value}`;
    } else if (value.length <= 6) {
      e.target.value = `(${value.slice(0, 2)}) ${value.slice(2)}`;
    } else {
      e.target.value = `(${value.slice(0, 2)}) ${value.slice(2, 7)}-${value.slice(7)}`;
    }
  });

  document.getElementById("cpf").addEventListener("input", function(e) {
    let value = e.target.value.replace(/\D/g, "");
    if (value.length > 11) value = value.slice(0, 11);
    
    if (value.length === 0) {
      e.target.value = "";
    } else if (value.length <= 3) {
      e.target.value = value;
    } else if (value.length <= 6) {
      e.target.value = `${value.slice(0, 3)}.${value.slice(3)}`;
    } else if (value.length <= 9) {
      e.target.value = `${value.slice(0, 3)}.${value.slice(3, 6)}.${value.slice(6)}`;
    } else {
      e.target.value = `${value.slice(0, 3)}.${value.slice(3, 6)}.${value.slice(6, 9)}-${value.slice(9)}`;
    }
  });

  function validarCPF(cpf) {
    cpf = cpf.replace(/\D/g, "");
    if (cpf.length !== 11) return false;
    if (/^(\d)\1{10}$/.test(cpf)) return false;
    
    let soma = 0;
    for (let i = 0; i < 9; i++) {
      soma += parseInt(cpf[i]) * (10 - i);
    }
    let resto = soma % 11;
    let digito1 = resto < 2 ? 0 : 11 - resto;
    if (parseInt(cpf[9]) !== digito1) return false;
    
    soma = 0;
    for (let i = 0; i < 10; i++) {
      soma += parseInt(cpf[i]) * (11 - i);
    }
    resto = soma % 11;
    let digito2 = resto < 2 ? 0 : 11 - resto;
    if (parseInt(cpf[10]) !== digito2) return false;
    
    return true;
  }

  function validarEmail(email) {
    const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return regex.test(email);
  }

  function validarCelular(celular) {
    const numero = celular.replace(/\D/g, "");
    return numero.length === 11;
  }

  document.getElementById("wifiForm").addEventListener("submit", async function(e) {
    e.preventDefault();

    const nome    = document.getElementById("nome");
    const email   = document.getElementById("email");
    const celular = document.getElementById("celular");
    const cpf     = document.getElementById("cpf");
    let valid = true;

    [nome, email, celular, cpf].forEach(f => {
      f.classList.remove("error-field");
    });
    document.querySelectorAll(".error-msg").forEach(el => el.style.display = "none");

    if (!nome.value.trim() || nome.value.trim().split(" ").length < 2) {
      nome.classList.add("error-field");
      document.getElementById("erroNome").style.display = "block";
      valid = false;
    }

    if (!validarEmail(email.value.trim())) {
      email.classList.add("error-field");
      document.getElementById("erroEmail").style.display = "block";
      valid = false;
    }

    if (!validarCelular(celular.value)) {
      celular.classList.add("error-field");
      document.getElementById("erroCelular").style.display = "block";
      valid = false;
    }

    if (!validarCPF(cpf.value)) {
      cpf.classList.add("error-field");
      document.getElementById("erroCpf").style.display = "block";
      valid = false;
    }

    if (!valid) return;

    const btn = document.getElementById("btnConectar");
    btn.textContent = "Conectando...";
    btn.classList.add("loading");
    btn.disabled = true;

    const payload = {
      nome:      nome.value.trim(),
      email:     email.value.trim().toLowerCase(),
      celular:   celular.value.replace(/\D/g, ""),
      cpf:       cpf.value.replace(/\D/g, ""),
      mac:       clientMac,
      timestamp: new Date().toISOString(),
      empresa:   "Flow Executive Finders"
    };

    try {
      await fetch(APPS_SCRIPT_URL, {
        method: "POST",
        mode: "no-cors",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload)
      });
    } catch (err) {
      console.warn("Erro ao registrar dados:", err);
    }

    document.getElementById("formSection").style.display = "none";
    document.getElementById("successScreen").style.display = "block";
  });
</script>

</body>
</html>
